
interface PaginationProductsProps {
  currentPage: number;
}

export default function PaginationProducts({ currentPage }: PaginationProductsProps) {
  return (
    <nav className="flex justify-center py-10">
      pagination
    </nav>
  )
}